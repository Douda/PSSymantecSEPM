commit-message-incrementing: MergeMessageOnly
branches:
  main:
    increment: Patch
  pull-request:
    label: rc
    increment: Patch
  develop:
    mode: ContinuousDeployment
    label: beta
    increment: Patch
ignore:
  sha: []
merge-message-formats: {}
