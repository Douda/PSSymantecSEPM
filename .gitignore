# ignore the build folder
Output/
Data/
Aurelien/
# Ignore version number folders and "output" where we build
[0-9]*/
gitversion.exe
# Ignore Thunder client
thunder-tests/
# VS Code user-specific files (not shared)
.vscode/*.code-snippets
# Keep settings.json and launch.json — they're shared config
Windows/
node_modules/
